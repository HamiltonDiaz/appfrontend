import api from "../lib/axios";

export async function createUser(user) {
    console.log(user);

    // try {
    //     const { data } = await api.post('/nnn', user)
    // } catch (error) {

    // }

}